spring:
  data:
    redis:
      client-type: lettuce
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
      ssl:
        enabled: false
      host: localhost
      port: 6379
      password: redis
      username: redis

redis-auto-config:
    disabled: false